<h1>Users</h1>

<table>
    <thead>
        <tr>
            <th>
                <strong>User Name</strong>
            </th>
            <th colspan="1"></th>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user| %>
            <tr>
                <td><%= user.name %></td>
                <td><%= link_to 'Make Admin', make_admin_path(user), method: :patch %></td>
            </tr>
        <% end %>
    </tbody>
</table>